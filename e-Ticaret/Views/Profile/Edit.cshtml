@using App.Data.Entities
@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor


@{
	var userName = HttpContextAccessor.HttpContext.Session.GetString("user");
	var isAuthenticated = !string.IsNullOrEmpty(userName);
}



@* !!!!DÜZENLENECEK!!!! *@

@* @if (isAuthenticated)
{
	var user = System.Text.Json.JsonSerializer.Deserialize<UserEntity>(userName);
	<div class="row">
		<div class="col-2">
		</div>
		<div class="col-8">
		

			<h2>Kategori Güncelle</h2>

			<form id="createCategoryForm" asp-controller="Profile" asp-action="Edit" method="post">
				<div class="form-group">
					<label class="form-label" asp-for="Name"></label>
					<input asp-for="Name" class="form-control" />
					<span class=" text-danger" asp-validation-for="Name"></span>
				</div>
				<div class="form-group">
					<label class="form-label" asp-for="Tanim">Tanım</label>
					<textarea class="form-control" asp-for="Tanim"></textarea>
					<span class=" text-danger" asp-validation-for="Tanim"></span>
				</div>
				<div class="form-group">
					<label class="form-label" asp-for="Color">Kategori Rengi</label>
					<textarea class="form-control" asp-for="Color"></textarea>
					<span class=" text-danger" asp-validation-for="Color"></span>
				</div>
				<div class="form-group">
					<label class="form-label" asp-for="IconCssClass">Kategori İkonu</label>
					<textarea class="form-control" asp-for="IconCssClass"></textarea>
					<span class=" text-danger" asp-validation-for="IconCssClass"></span>
				</div>
				<div class="row text-center">

					<div class="form-group col-12">
						<button type="submit" class="btn btn-success">Kategoriyi Güncelle</button>
						<a type="button" class="btn btn-danger" asp-controller="Category" asp-action="List">Geri Dön</a>
					</div>
				</div>
			</form>
		</div>
	</div>
	<div class="row">
		<div class="col-4"></div>
		<div class="col-8">
			<a asp-controller="Auth" asp-action="Logout"><i class="fa fa-sign-out site-btn">  Çıkış</i> </a>
			<a asp-controller="Home" asp-action="Index"><i class="fa fa-home site-btn">  Ana Sayfa</i> </a>
		</div>
	</div>
}
else
{
	<br />
	<br />

	<div class="text-center">
		<h4>Lütfen giriş yapın!! </h4>
	</div>
	<br />
	<br />

	<div class="text-center">
		<a asp-controller="Auth" asp-action="Login"><i class="site-btn"></i> Giriş Yap</a>
	</div>
} *@







